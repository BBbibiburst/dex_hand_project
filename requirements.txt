# Core simulation, numerical stack, and native GraspQP + DexEvolve synthesis
numpy
scipy
trimesh
mujoco
gymnasium
torch
graspqp @ git+https://github.com/leggedrobotics/graspqp.git#subdirectory=graspqp
proxsuite>=0.7

# Project-native GraspQP + DexEvolve and MJWarp PPO stack. Third-party source trees are
# not vendored in this repository; install all runtime dependencies with pip.
mujoco-warp>=3.11.0
warp-lang>=1.14

# Visualization used by examples, diagnostics, and tactile tools
matplotlib
opencv-python
Pillow

# Reproducible YCB asset download (EGAD uses the Python standard library)
mani-skill

# Imitation learning and LeRobot datasets (torch is required above by GraspQP + DexEvolve)
torchvision
torchcodec
lerobot>=0.4

# Vive / SteamVR hardware
openvr

# Bluetooth glove (Windows COM/SPP and direct RFCOMM support)
# Linux prerequisite: libbluetooth-dev (Debian/Ubuntu) or bluez-libs-devel (Fedora/RHEL)
pyserial
PyBluez @ git+https://github.com/pybluez/pybluez.git

# Development and architecture regression checks
ruff
pytest
